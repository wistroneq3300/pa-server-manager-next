# Appearance update — verification record

Date: 2026-09-21. Local loopback fixture preview only, not production hardware.

## Executed browser acceptance

- `qa/acceptance.cjs`: **23/23 passed** after the final renderer, Light stylesheet and focus correction. See `acceptance.json` for the generated timestamp and individual results.
- Current Dashboard, Rack-stage, System Detail and Rack Workspace PNGs were regenerated by that run.
- Browser exceptions: 0. Console errors: 0. External requests: 0 in that acceptance context.

## Direct desktop-browser inspection

These checks used the connected desktop browser, separately from the test runner.

| CSS viewport width | Light routes | Dark routes | Document horizontal overflow |
| --- | --- | --- | --- |
| 1440 | Dashboard, Projects, System Detail, Rack | Same four | None |
| 1600 | Dashboard, Projects, System Detail, Rack | Same four | None |
| 1920 | Dashboard, Projects, System Detail, Rack | Same four | None |

24 width/theme/route combinations were measured using document client/scroll widths. Actual CSS widths were checked independently of operating-system display scaling.

- Pearl Light uses visibly white/silver surfaces; Graphite Dark remains dark. Light preference survived a full page reload.
- Real Compute Tray geometry changes orientation under pointer drag and arrow keys; released orientation stays unchanged. Rear and reset controls work.
- Full Rack assembly, rear spine and heavily tilted rack were inspected. The same canvas carries the scroll narrative in both directions.
- Aggressive L10 rotation at yaw -1.52 / pitch -1.134 increased camera distance to 16.8137 and kept the complete tray visible.
- Fixed a discovered keyboard-focus issue: the sticky stage used to scroll internally by about 78 px horizontally. After `overflow:clip`, its internal `scrollLeft` and `scrollTop` remained 0 under keyboard focus and drag.
- Light System Detail: Overview, Hardware, Sensors, Telemetry and Test tasks inspected. Hardware summaries reflect the fixture inventory, not a fixed GPU product label.
- 13 visible Telemetry canvases had nonzero dimensions. Theme switching updated chart text/grid contrast.
- Test library displayed six categories / 3,112 cases. The 60-case category dialog, search and close controls were inspected in Light.
- Light 48U rack, component surfaces and topology inspected. Fixed pale rack/project description text inherited from the dark stylesheet.
- Connected-browser error log was empty after the width/route checks.

## Executed portable checks

- `node qa/hardware-identity.cjs --unit`: pass.
- `node qa/theme-contract.cjs`: 5/5 pass; 303 Light-scoped rules and 18 sampled text/background contrast pairs.
- `node qa/theme-palette.cjs`: pass against the bundled Chart.js implementation.
- `node qa/story-adapter.cjs`: lifecycle and repeated-mount tests pass.
- `node qa/core-scene.cjs`: 7/7 pass. This uses mocked WebGL, not pixel assertions.
- Original `static/js/app.js` SHA256 unchanged: `05A5E7916C0C104CA536AF85FB324E58EF9288ABB1B81A72D93799798FFA9F3C`.

## Explicit test limitations

The separate Chrome launch for `qa/appearance-interaction.cjs` and full-browser `qa/hardware-identity.cjs` was denied by the execution environment. These supplementary browser suites were **not executed successfully and are not counted as passed**. They remain available for future local runs. The successful existing acceptance runner, direct desktop inspection and portable tests are the evidence reported above.

No full accessibility conformance, physical CAD accuracy or production FastAPI/equipment integration is claimed.
